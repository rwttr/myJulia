#using VMLS
#using LinearAlgebra
using Images#, ImageView
using FileIO

#img = testimage("mandrill")
img = load("testimage_RGB1.png")

#sizeof(var) : size of variable in byte
#size(var) : return a tuple = size of matrix